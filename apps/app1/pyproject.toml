[project]
name = "app1"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "Alex Xela", email = "h825@pm.me"},
]
dependencies = [
    "fastapi",
    "pkg-database @ file:///${PROJECT_ROOT}/../../packages/pkg-database",
    "uvicorn"
]
requires-python = "==3.11.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
